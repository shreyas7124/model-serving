# Core dependencies for all applications
# Install application-specific requirements from each app's directory

# Shared dependencies
streamlit>=1.28.0
transformers>=4.35.0
torch>=2.0.0
accelerate>=0.24.0
flask>=3.0.0
flask-cors>=4.0.0
flask-socketio>=5.3.0
python-socketio>=5.10.0
requests>=2.31.0
waitress>=3.0.0
redis>=5.0.0

# Optional: Install all at once
# Or install per-application using their individual requirements.txt files

