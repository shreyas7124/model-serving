# Core dependencies for application frontends
# vLLM is deployed separately (see deploy/docker-compose.vllm.yml)
# Install application-specific requirements from each app directory

streamlit>=1.28.0
flask>=3.0.0
flask-cors>=4.0.0
flask-socketio>=5.3.0
python-socketio>=5.10.0
requests>=2.31.0
waitress>=3.0.0
redis>=5.0.0

# NIM apps still use the same HTTP client stack (no local torch required for HF path)
# Optional Mooncake L2 response cache (hf-ide-assistant, Moonshot/Kimi ids):
#   pip install mooncake-transfer-engine
